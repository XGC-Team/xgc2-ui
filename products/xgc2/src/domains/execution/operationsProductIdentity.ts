import { defineProductOwnerIdentity } from '../../shared/productWebComposition';

/** Canonical Product.Operations web owner identity for composition dependencies. */
export const productOperationsOwnerIdentity = defineProductOwnerIdentity('Product.Operations');
