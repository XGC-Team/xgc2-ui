---
"@xgc2/ui-react": patch
"@xgc2/ui-policy": patch
---

Render the sidebar collapse affordance as a centered SVG with a fixed icon box so typography changes cannot distort its size or alignment. Preserve the sidebar width transition and accessible toggle identity.

Allow DataTable columns to identify the actual header cell and sort button while retaining shared sorting behavior and accessibility ownership.

Keep the policy package pinned to the matching React patch release.
