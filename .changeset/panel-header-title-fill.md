---
'@xgc2/ui-react': patch
---

Panel and WorkspacePanel header titles fill the header content row: the title line box uses --size-control-panel-header so the label has equal gaps on every side (the header --space-xs padding) and aligns with the header's compact controls.
