---
"@xgc2/ui-tokens": patch
"@xgc2/ui-react": patch
"@xgc2/ui-policy": patch
---

Keep destructive command text on the theme's danger scale and readable through idle, hover, active, and held states. Preserve the command's pressed relief, neutral icon-only surfaces, and disabled progress identity. Pair the policy package with the matching React patch.
